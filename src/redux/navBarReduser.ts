let initialState = {
  friends: [
    {id: 1, name: "Viktor"},
    {id: 2, name: "Sasha"},
    {id: 3, name: "Gleb"},
  ],
}

const navBarReducer = (state = initialState, action: any) => {
  return state;
};

export default navBarReducer;
